<% if @user.admin %>
	<p>ADMIN</p>
	<h4><%= @user.name %></h4><br>
	<% else %>
		<p>Welcome <%= @user.name %></p>
	<% end %>	
	
	<%= link_to 'See Budgets', budgets_path %><br>
	<%= link_to 'Log Out', logout_path, method: :post %>